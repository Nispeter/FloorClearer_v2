using UnityEngine;

public class HUBGameManager : MonoBehaviour
{
    /*public GameObject Hub;
    public GameObject PlayerPrefab; 
    public Transform PlayerSpawnPoint; 

    private GameObject playerInstance;

    private void Start()
    {
        ActivateHub();
        SpawnPlayer();
    }

    public void ActivateHub()
    {
        if (Hub != null)
        {
            Hub.SetActive(true);
        }
        else
        {
            Debug.LogError("Hub not assigned in GameManager.");
        }
    }

    public void SpawnPlayer()
    {
        if (PlayerPrefab != null && PlayerSpawnPoint != null)
        {
            playerInstance = Instantiate(PlayerPrefab, PlayerSpawnPoint.position, PlayerSpawnPoint.rotation);
        }
        else
        {
            Debug.LogError("PlayerPrefab or PlayerSpawnPoint not assigned in GameManager.");
        }
    }*/
}
